# Copy to .env.local for local work, and set the same names in the Vercel
# project (Settings -> Environment Variables) for the live site.
# .env* is gitignored except this file, so real values are never committed.

# The public address of the site. Canonical URLs, Open Graph tags and the
# sitemap are all resolved against this, so it must be the real domain.
# REQUIRED on Vercel. Must be the address the site actually answers on,
# with scheme and no trailing slash. If it is missing the build falls back to
# the .vercel.app production URL, which is correct but not the address you want
# indexed once a real domain is attached.
NEXT_PUBLIC_SITE_URL=https://yumegakanau.in

# --- Trial-form email (Nodemailer over Gmail SMTP) ---
# GMAIL_APP_PASSWORD is a 16-character Google App Password, NOT the Gmail
# password. Generate one at https://myaccount.google.com/apppasswords
# (2-Step Verification must be on first). It can be revoked on its own.
GMAIL_USER=yumegakanau22@gmail.com
GMAIL_APP_PASSWORD=
# Where enquiries land. Defaults to GMAIL_USER if left blank.
CONTACT_TO=yumegakanau22@gmail.com

# --- Google Search Console ---
# Paste the token from the "HTML tag" verification method (the content="..."
# value only). Leave blank until you have it.
GOOGLE_SITE_VERIFICATION=

# --- Optional: use a different mail provider ---
# Leave these unset to use Gmail. Set SMTP_HOST/SMTP_PORT to point the same
# route at any other SMTP server instead.
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_SECURE=false

# --- Sensei's document console (/admin) ---
# The access token Sensei types to open /admin. There is one person and one
# token, so make it long and random rather than memorable — a short one can be
# guessed over the internet, a 32-character random one cannot.
# Generate one with:  node -e "console.log(require('crypto').randomBytes(24).toString('base64url'))"
# Changing this signs every device out at once.
ADMIN_TOKEN=

# The key that signs generated document links. Every /d/... link carries the
# values Sensei typed, stamped with this secret so nobody can edit a fee or a
# batch code in the URL. Generate it the same way as ADMIN_TOKEN.
#
# Keep it SEPARATE from ADMIN_TOKEN: that way changing the admin password does
# not break links already shared with students. If this is left blank the app
# falls back to ADMIN_TOKEN, which works but ties the two together.
#
# Changing this invalidates every link ever issued. That is the emergency stop.
FORM_SIGNING_SECRET=
